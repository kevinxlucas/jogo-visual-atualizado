window.VisualTrainingConfig = window.VisualTrainingConfig || {
  // Depois de publicares o Google Apps Script como Web App, cola aqui o URL /exec.
  // Também podes configurar pela interface do Dashboard; fica guardado em localStorage.
  apiUrl: ''
};
